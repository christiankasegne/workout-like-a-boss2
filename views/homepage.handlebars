
<h2 id="home-phrase">Chose Your Muscle Group!</h2>
{{#each muscleGroups as |muscleGroup|}}
<div class="container-fluid" id="muscleCard">  
    <div class="row gy-3 justify-content-center">
        <div class="col-md-9" style="margin-top: 30px;">
            <div class="card h-130">
                <div class="card" id="c">
                    <img src="{{muscleGroup.images}}" class="card-img-top" alt="Picture">
                    <div class="card-body">
                        <h5 class="card-title">{{muscleGroup.muscleGroup}}</h5>
                        <a href="/workout/{{muscleGroup.id}}" class="btn btn-dark">See Workouts!</a>
                    </div>
                </div>
            </div>
        </div>
    </div> 
</div>

{{/each}}